Uses SOAP  1.1

This example provides the web interface by annotating the implementation class,
it does not use a separate interface definition


This example creates a web service using SOAP 1.1

Then we create a SOAP 1.1 message using SAAJ.

Finally, we send this message using a dispatch client.
